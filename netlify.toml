# Deploy de la plataforma Next.js (vive en platform/).
# Con base="platform", Netlify construye la app aunque no se setee el base dir en la UI.
[build]
  base = "platform"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
